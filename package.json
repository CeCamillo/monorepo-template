{
  "name": "taskyard",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev:api": "bun --filter api-server dev",
    "dev:web": "bun --filter web-app dev",
    "dev": "bun --filter api-server dev & bun --filter web-app dev"
  }
}
